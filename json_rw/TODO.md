# TODO

- [x] #1 Support `Stream<List<int>>` parsing.
  - [x] Read a `Stream<List<int>>` directly (using the new exception).
  - [x] Support `Stream<List<int>>` writing.

- [ ] **Code Generation Integration**
  - Plan how to generate `ResumableBuilder` classes and `.rw.dart` files automatically.
