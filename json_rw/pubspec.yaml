name: json_rw
description: Streaming encode/decode of JSON without intermediate objects.
version: 0.1.0-dev
resolution: workspace
environment:
  sdk: ^3.9.0
dev_dependencies:
  checks: ^0.3.1
  test: ^1.31.0
